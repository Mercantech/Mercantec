services:
  api:
    ports:
      - "${API_PORT:-4041}:4041"

  postgres:
    ports:
      - "${POSTGRES_PORT:-5433}:5432"

  minio:
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
